"""
题目 016: 检查字符串是否包含特定子串

要求:
编写一个名为 `contains_substring` 的函数，它接受两个字符串 `s` 和 `sub` 作为参数。如果 `s` 包含 `sub`，则返回 `True`，否则返回 `False`。

提示:
和检查字典的键类似，你也可以使用 `in` 关键字来检查子串。
"""

def contains_substring(s, sub):
    # 在这里写下你的代码
    pass
