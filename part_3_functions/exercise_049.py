"""
题目 049: 安全地拼接文件路径

要求:
编写一个名为 `join_path` 的函数，它接受两个路径部分 `part1` 和 `part2`。
函数应使用 `os` 模块返回一个在所有操作系统上都正确的组合路径。

提示:
`import os`，然后使用 `os.path.join(part1, part2)`。
这比简单地用 `+` 拼接字符串更安全、更跨平台。
"""
import os

def join_path(part1, part2):
    # 在这里写下你的代码
    pass
