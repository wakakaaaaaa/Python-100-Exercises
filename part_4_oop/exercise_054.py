"""
题目 054: 创建“创建待办事项”的端点

要求:
修改 `part_4_oop/main.py` 文件。
创建一个 `POST /todos` 的端点。它应该:
1. 接受一个不含 `id` 的待办事项作为请求体。
2. 创建一个新的 `Todo` 对象，并为其分配一个唯一的 `id`。
3. 将新的 `Todo` 添加到 `db` 列表中。
4. 返回新创建的 `Todo` 对象，状态码应为 201。

提示:
为了简化 `id` 的生成，你可以假设新的 `id` 就是当前 `db` 列表的长度加1。
使用 `@app.post("/todos", status_code=201)`。
"""
