"""
题目 057: 创建“删除待办事项”的端点

要求:
修改 `part_4_oop/main.py` 文件。
创建一个 `DELETE /todos/{todo_id}` 的端点，用于删除一个待办事项。
- 如果找到并成功删除，返回一个确认信息，例如 `{"message": "Todo deleted"}`。
- 如果未找到，返回 404 错误。

提示:
找到列表中的对象后，可以使用 `.remove()` 方法将其删除。
"""
