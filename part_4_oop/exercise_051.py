"""
题目 051: Setup FastAPI and a "Hello World" Endpoint

要求:
修改 `part_4_oop/main.py` 文件。
创建一个 FastAPI 实例，并添加一个根路由 `/`。
这个路由应该通过一个GET请求访问，并返回一个JSON对象 `{"message": "Hello, World!"}`。

提示:
你需要使用 `@app.get("/")` 装饰器来定义路由。
"""
