"""
题目 093: 完善API文档

要求:
回到你的代码中（例如 `routers/todos.py` 和 `schemas.py`），
为你的API端点和Pydantic模型添加 `description`, `summary`, `tags` 和 `example` 等信息。
完成后，运行应用并访问 `/docs` 查看效果。

提示:
`@router.post("/", summary="Create a new todo", ...)`
`title: str = Field(..., example="Buy milk")`
"""
