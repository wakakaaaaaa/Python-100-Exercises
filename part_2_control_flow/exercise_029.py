"""
题目 029: 统计句子中的词频

要求:
编写一个名为 `word_count` 的函数，它接受一个句子（字符串）`sentence` 作为参数。
函数应返回一个字典，其中键是句子中的单词，值是该单词出现的次数。单词不区分大小写。

提示:
你需要先用 `.lower()` 将句子转为小写，然后用 `.split()` 将其分割成单词列表。
之后，遍历这个列表并用一个字典来计数。
"""

def word_count(sentence):
    # 在这里写下你的代码
    pass
