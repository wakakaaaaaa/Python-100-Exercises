"""
题目 021: FizzBuzz

要求:
编写一个名为 `fizzbuzz` 的函数，它接受一个整数 `n`。函数应返回一个列表，包含从1到 `n` 的数字。
但是，如果数字是3的倍数，则用字符串 `"Fizz"` 替换该数字；
如果是5的倍数，则用 `"Buzz"` 替换；
如果既是3的倍数也是5的倍数，则用 `"FizzBuzz"` 替换。

提示:
这是一个经典的编程挑战。你需要使用循环和多个 `if/elif/else` 条件。
判断一个数是否是另一个数的倍数，取模运算符 `%` 会很有用。
"""

def fizzbuzz(n):
    # 在这里写下你的代码
    pass
