"""
题目 026: 查找元素在列表中的索引

要求:
编写一个名为 `find_index` 的函数，它接受一个列表 `items` 和一个元素 `element` 作为参数。
返回该元素在列表中首次出现的索引。如果元素不存在，则返回 `-1`。

提示:
虽然列表有 `.index()` 方法，但它在元素不存在时会抛出异常。
尝试自己用 `for` 循环和 `enumerate()` 函数来实现这个逻辑，这样可以更好地控制返回 `-1` 的情况。
"""

def find_index(items, element):
    # 在这里写下你的代码
    pass
