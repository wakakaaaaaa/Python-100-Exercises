"""
题目 034: 创建一个简单的银行账户类

要求:
创建一个 `BankAccount` 类。它在初始化时接受一个 `initial_balance`（初始余额）。
类应包含以下方法:
- `get_balance()`: 返回当前余额。
- `deposit(amount)`: 存入指定金额。
- `withdraw(amount)`: 取出指定金额。

提示:
`deposit` 会增加余额，`withdraw` 会减少余额。暂时不用考虑余额不足的情况。
"""

class BankAccount:
    # 在这里写下你的代码
    pass
